namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Ação do fluxo de liberação de documento a pagar. Épico E4.
/// Origem: Projeto.Geral.EnumeradoObjeto.AcaoLiberarDoctoPagar (legado) — não-persistido
/// (comportamento em memória, nunca vira coluna).
/// </summary>
public enum AcaoLiberarDoctoPagar
{
    Liberar = 1,
    Cancelar = 2
}
