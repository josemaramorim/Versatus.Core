namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Sinal de um movimento (entrada/saída).
/// Origem: Projeto.Geral.EnumeradoObjeto.EntradaSaida (legado) — não-persistido
/// (comportamento em memória, nunca vira coluna).
/// </summary>
public enum EntradaSaida
{
    Entrada = 1,
    Saida = 2
}
