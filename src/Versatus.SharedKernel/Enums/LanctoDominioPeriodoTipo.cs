namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Tipo de lançamento de período de domínio (abertura, fechamento, suprimento, sangria).
/// Épico E2.
/// Origem: Projeto.Geral.Enumerado.LanctoDominioPeriodoTipo (legado, [TipoEnumerado(262)]) —
/// persistido em FINDOMINIOPERIODOLACTO.
/// </summary>
public enum LanctoDominioPeriodoTipo
{
    Abertura = 263,
    Fechamento = 264,
    Suprimento = 290,
    Sangria = 291
}
