namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Ação do fluxo de aprovação de documento a pagar. Épico E4.
/// Origem: Projeto.Geral.EnumeradoObjeto.AcaoAprovarDoctoPagar (legado) — não-persistido
/// (comportamento em memória, nunca vira coluna).
/// </summary>
public enum AcaoAprovarDoctoPagar
{
    Aprovar = 1,
    Cancelar = 2
}
