namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Situação de um movimento financeiro (caixa/banco).
/// Origem: Projeto.Geral.Enumerado.SituacaoMovimento (legado, [TipoEnumerado(287)]) —
/// persistido em FINMOVIMENTO.IDSITUACAO.
/// </summary>
public enum SituacaoMovimento
{
    Normal = 288,
    Cancelado = 289
}
