namespace Versatus.SharedKernel.Enums;

/// <summary>
/// Operação de conversão por índice econômico (multiplicação/divisão). Épicos E1/E3
/// (golden tests de conversão).
/// Origem: Projeto.Geral.Enumerado.OperacaoConversao (legado, [TipoEnumerado(265)]) —
/// persistido em FININDICECONVERSOR.
/// </summary>
public enum OperacaoConversao
{
    Nenhuma = 266,
    Multiplicacao = 267,
    Divisao = 268
}
