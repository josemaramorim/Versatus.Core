Extraído de localhost\SQLEXPRESS2008 / versatus (SQL Server 2008 R2) em 2026-09-03 via INFORMATION_SCHEMA.

fin_columns.txt: TABELA|COLUNA|ORDINAL|DATA_TYPE|IS_NULLABLE|CHAR_MAXLEN|NUM_PRECISION|NUM_SCALE (todas as tabelas BASE `Fin%`).
fin_meta.txt: PKs (com ordem das colunas), contagem de linhas, views `vw*` relacionadas, colunas de GloManutencaoRateio.

Ground truth do `data-model.md`. Nomes fisicos sao MAIUSCULOS e diferem dos atributos [TableName]/[AutoSequencial] do Gentle no legado.
