---
name: create-api-module
description: Use ao criar um novo módulo de negócio no backend C# (.NET 10) para estruturar pastas, DbContexts (Write/Read) e injeção de dependência.
---

# Skill: create-api-module (stub de descoberta)

> Este arquivo existe só para o Claude Code descobrir e disparar esta skill automaticamente.
> A fonte única e completa das instruções é `.agents/skills/create-api-module/SKILL.md` —
> não duplicar conteúdo aqui.

**Ação obrigatória ao disparar esta skill:** leia agora, na íntegra,
`.agents/skills/create-api-module/SKILL.md` e o template em
`.agents/skills/create-api-module/templates/DbContextTemplate.cs` antes de prosseguir. Siga
rigorosamente o que está lá — este stub não substitui essa leitura.
