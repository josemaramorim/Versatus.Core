---
name: spec-generator
description: Use ao mapear UMA tela/formulário legado isolado para elaborar sua especificação funcional em docs/spec_f[nome].md antes de escrever código C# ou React. Para escopo de módulo inteiro use o fluxo SDD (sdd-specify).
---

# Skill: spec-generator (stub de descoberta)

> Este arquivo existe só para o Claude Code descobrir e disparar esta skill automaticamente.
> A fonte única e completa das instruções é `.agents/skills/spec-generator/SKILL.md` — não
> duplicar conteúdo aqui.

**Ação obrigatória ao disparar esta skill:** leia agora, na íntegra,
`.agents/skills/spec-generator/SKILL.md` e os arquivos de referência em
`.agents/skills/spec-generator/references/` (`sql_nullability_guide.md`,
`template_spec_fentidade.md`) antes de prosseguir. Siga rigorosamente o que está lá — este
stub não substitui essa leitura.
